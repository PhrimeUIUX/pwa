{
  "version": 2,
  "builds": [
    { "src": "build/web/**", "use": "@vercel/static" }
  ],
  "rewrites": [
    {
      "source": "/:page",
      "destination": "/build/web/:page.html"
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/build/web/$1" }
  ]
}

